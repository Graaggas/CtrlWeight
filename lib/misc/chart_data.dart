class WeightChart {
  final DateTime dateTime;
  final double weight;

  WeightChart({this.dateTime, this.weight});
}

// final List<WeightChart> fakeData = [
//   WeightChart(dateTime: DateTime.now().toString(), weight: 200),
//   WeightChart(dateTime: DateTime.now().toString(), weight: 220),
//   WeightChart(dateTime: DateTime.now().toString(), weight: 120),
// ];
